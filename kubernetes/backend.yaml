---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: webapp
spec:
  replicas: 3
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: webapp
        image: quelconteneur:v1
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: webapp-svc
  annotations:
    traefik.ingress.kubernetes.io/affinity: "true"
  labels:
    app: webapp
    
spec:
  ports:
  - port: 80
  selector:
    app: webapp
